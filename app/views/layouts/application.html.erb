<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

 <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %> Markibles</title>

  <%= stylesheet_link_tag "application" %>
  <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <nav class="top-bar" data-topbar>
      <ul class="title-area">
        <li class="name">
          <h1><%= link_to 'Markibles', root_path %></h1>
        </li>
        <li class="toggle-topbar menu-icon"><a href="#">Menu</a></li>
    </ul>

    <section class="top-bar-section">
    <!-- Right Nav Section -->
    <ul class="right">
      <%- if user_signed_in? -%>
        <li class="active"><%= link_to 'Sign Out', destroy_user_session_path, method: :delete %></li>
      <%- else -%>
      <li><%= link_to 'Sign In', new_user_session_path %></li>
      <li><%= link_to 'Sign Up', new_user_registration_path %>
      <%- end -%>

      <li class="has-dropdown">
        <a href="#">Feed or Be Fed</a>
        <ul class="dropdown">
          <li><%= link_to "List a Food Item!", new_product_path  %></li>
          <li><%= link_to "Create a store", new_seller_path  %></li>
          <li><%= link_to "Find an item", products_path  %></li>
          <li> <%= link_to "Contact Us!", new_message_path%></li>
        </ul>
      </li>


    </li>
  </section>
 </ul>
</nav>

  <p class="notice"><%= notice %></p>
  <p class= "alert"><%= alert %></p>

<%= yield %>
<%= javascript_include_tag "application" %>


  </body>
</html>
